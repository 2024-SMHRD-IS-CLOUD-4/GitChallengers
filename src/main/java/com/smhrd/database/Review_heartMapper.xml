<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Review_heartMapper">
		<!-- 좋아요 여부 확인 -->
	<select id="likeCheck" parameterType="Review_heart" resultType="int">
		select count(*) from review_heart where review_idx=#{review_idx} and id=#{id}
	</select>
	
	<!-- 좋아요 추가 -->
	<insert id="addLike" parameterType="Review_heart">
		insert into review_heart values (review_heart_seq.nextval, #{review_idx}, #{id}, sysdate)
	</insert>
	
	<!-- 좋아요 삭제 -->
	<delete id="deleteLike" parameterType="Review_heart">
		delete from review_heart where review_idx=#{review_idx} and id=#{id}
	</delete>
	
	<!-- 좋아요 수 -->
	<select id="likeCount" resultType="int">
		select count(*) from review_heart where review_idx=#{review_idx}
	</select>

</mapper>